# Projeto
HTML
CSS
JS
Node
Espress
Sequelize
PostgresSQl
bcrypt

# npm intall
npm ini -y
npm intall express                                  // Express
npm install express-session                         // Controle de dados e telas
npm install sequelize pg pg-hstore                  // ORM Sequelize BD Postgres
npm install --save-dev sequelize-cli
npm install bcrypt                                  // Criptografa as senhas

-- Não usado
npx sequelize-cli init
